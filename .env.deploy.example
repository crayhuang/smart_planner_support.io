# Copy this file to .env.deploy and replace every placeholder.
# Never commit .env.deploy or the referenced SSH private key.
SITE_HOST=203.0.113.10
SITE_USER=smartplanner
SITE_PORT=22
SITE_DIR=/var/www/smart-planner
SITE_SSH_KEY=$HOME/.ssh/smart-planner_ed25519

